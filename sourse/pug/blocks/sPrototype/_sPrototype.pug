mixin sPrototype(data)
	// start sPrototype
	+b.SECTION.sPrototype.section#sPrototype&attributes(attributes)
		.container
			+b.section-title.text-center
				h2 sPrototype
				
			.row
	// end sPrototype